{% extends 'base.html.twig' %}

{% block title %}Edit Article{% endblock %}

{% block body %}
    <h1>Edit Article</h1>
    <div class="text-center">
        {{ include('article/_form.html.twig', {'button_label': 'Update'}) }}
        <a href="{{ path('article_index') }}" class="btn btn-primary mt-4">back to list</a>
    </div>


    {{ include('article/_delete_form.html.twig') }}
{% endblock %}
{% block javascripts %}
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script>
        const article = { 
            'title' : 'Lorem rhooooo', 
            'content' : 'lorem ipsum rhoooo'
        }
        axios.post('https://localhost:8000/ajax/articles/2/edit', article)
        .then(response => console.log(response.data))
    </script>
{% endblock %}